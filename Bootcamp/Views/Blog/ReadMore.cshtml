@model Bootcamp.Models.Blogs
@{
    ViewData["Title"] = "Blog Content";
}

<div class="Content">
    
        <div class="Title">
            <h1>@Model.Title</h1>
        </div>
        <div class="blog_img">
            @*<img src=@string.Format($"~{Model.Image}")/>*@
        <img src="@Model.Image">
        </div>
        <div class="big_content">
            @Model.BiggerContent
        </div>
</div>